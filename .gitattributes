* text=auto eol=lf

# Icons and other image binaries must not be treated as text.
*.png binary
*.ico binary
*.icns binary

# Preserve verified upstream filter and license bytes on every platform.
src-tauri/filters/*.txt -text
